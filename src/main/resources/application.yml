spring:
  application:
    name: NCHomework_5
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:noname@nc.com}
    password: ${EMAIL_PASSWORD:password}
    properties:
      mail.transport.protocol: smtp
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: true